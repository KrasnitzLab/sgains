genome:
  version: hg19
  data_dir: data/hg19_safe
  work_dir: data/hg19
  index: genomeindex

mappable_regions:
  length: 100
  work_dir: data/R100
  bowtie: ''
  mappable_regions: hg19_R100_mappable_regions.txt

bins:
  bins_count: 10000
  work_dir: data/R100_B10k
  bins_boundaries: hg19_R100_B10k_bins_boundaries.txt

mapping:
    reads_dir: reads
    reads_suffix: ".fastq.gz"
    mapping_dir: mapped
    mapping_suffix: ".rmdup.bam"
    mapping_bowtie_opts: "-S -t -n 2 -e 70 -3 0 -5 38 -m 1 --best --strata"

varbin:
    varbin_dir: varbin
    varbin_suffix: ".varbin.20k.txt"

segment:
    segment_dir: segment
    study_name: "test_study"
