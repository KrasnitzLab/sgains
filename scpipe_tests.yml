genome:
  version: hg19
  pristine: data/hg19_safe
  cache_dir: data/hg19
  index: genomeindex

reads:
  length: 100
  cache_dir: data/R100

bins:
  bins_count: 10000
  cache_dir: data/R100_B10k
  bin_boundaries: bin_boundaries.tsv

cells:
  cache_dir: data/kendall/ready
